# 部署配置示例
# 复制本文件为 .deploy.env 并填入实际服务器信息：
#   cp .deploy.sample.env .deploy.env
#
# .deploy.env 已被 .gitignore 忽略，不会提交到仓库

# SSH 用户@主机
SERVER=user@example.com

# SSH 端口（默认 22，可省略）
SERVER_PORT=22

# 远端目标目录（站点文件将上传到该目录）
SERVER_PATH=/var/www/timelynotify-doc
